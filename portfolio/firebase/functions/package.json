{
  "name": "functions",
  "description": "Firebase functions for portfolio",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint ."
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^7.0.0",
    "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.7",
    "typescript": "^5.2.2"
  }
}
